services:
  livekit:
    image: livekit/livekit-server:latest
    restart: unless-stopped
    ports:
      - "7880:7880"
      - "7881:7881"
      - "7882:7882/udp"
    command: --dev --bind "0.0.0.0" --redis-host redis:6379 --node-ip 89.116.34.144
    depends_on:
      redis:
        condition: service_started
    networks:
      - agent_network

  livekit-sip:
    image: livekit/sip:latest
    # ports:
    #   - "5060:5060/udp"
    #   - "5060:5060/tcp"
    #   - "10000-10100:10000-10100/udp"  # narrowed range for local dev
    network_mode: "host"  # Use host networking for SIP to avoid port conflicts
    volumes:
      - ./sip.yaml:/etc/livekit/sip.yaml
    command: --config /etc/livekit/sip.yaml
    depends_on:
      livekit:
        condition: service_started
    # networks:
    #   - agent_network

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - "6379:6379"
    networks:
      - agent_network

  sip_api:
    platform: linux/amd64
    build:
      context: ./livekit_agent
      dockerfile: Dockerfile.sip
    restart: unless-stopped
    ports:
      - "8089:8089"
    env_file:
      - .env
    environment:
      - REDIS_HOST=${REDIS_HOST:-redis}
      - REDIS_PORT=${REDIS_PORT:-6379}
    depends_on:
      redis:
        condition: service_started
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8089/docs > /dev/null || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 10s
    networks:
      - agent_network

  livekit_agent:
    platform: linux/amd64
    build:
      context: ./livekit_agent
    restart: unless-stopped
    volumes:
      - ./livekit_agent/src:/app/src
    env_file:
      - .env
    environment:
      - LIVEKIT_URL=${LIVEKIT_URL:-ws://livekit:7880}
      - LIVEKIT_HOST=${LIVEKIT_URL:-ws://livekit:7880}
      - LIVEKIT_API_KEY=${LIVEKIT_API_KEY:-devkey}
      - LIVEKIT_API_SECRET=${LIVEKIT_API_SECRET:-secret}
      - LIVEKIT_AGENT_PORT=7880
      - DEEPGRAM_API_KEY=${DEEPGRAM_API_KEY}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - GROQ_API_KEY=${GROQ_API_KEY}
      - ELEVEN_API_KEY=${ELEVEN_API_KEY}
      - TAVILY_API_KEY=${TAVILY_API_KEY}
      - REDIS_HOST=${REDIS_HOST:-redis}
      - REDIS_PORT=${REDIS_PORT:-6379}
    depends_on:
      livekit:
        condition: service_started
      redis:
        condition: service_started
      # kokoro:
      #   condition: service_started
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8081/ > /dev/null"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 10s
    networks:
      - agent_network

  frontend:
    build:
      context: ./frontend
      args:
        NEXT_PUBLIC_LIVEKIT_URL: ${NEXT_PUBLIC_LIVEKIT_URL:-ws://localhost:7880}
    restart: unless-stopped
    ports:
      - "3033:3000"
    environment:
      - NEXT_PUBLIC_LIVEKIT_URL=${NEXT_PUBLIC_LIVEKIT_URL:-ws://localhost:7880}
      - LIVEKIT_URL=ws://livekit:7880
      - LIVEKIT_API_KEY=devkey
      - LIVEKIT_API_SECRET=secret
    depends_on:
      livekit:
        condition: service_started
      livekit_agent:
        condition: service_started
      sip_api:
        condition: service_healthy
    networks:
      - agent_network

  # kokoro:
  #   build:
  #     context: ./inference/kokoro
  #   ports:
  #     - "8880:8880"
  #   networks:
  #     - agent_network

networks:
  agent_network:
    driver: bridge